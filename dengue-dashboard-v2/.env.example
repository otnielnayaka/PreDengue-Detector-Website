# Backend API base URL
# Default uses Vite proxy (recommended for development)
VITE_API_URL=/api/v1

# To bypass proxy and call backend directly (e.g. production):
# VITE_API_URL=http://127.0.0.1:8000/api/v1
